name: al_najjar_app
description: A Flutter project for Carpenter Services.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0 # لتحسين الخط العربي
  font_awesome_flutter: ^10.6.0 # لأيقونات التواصل الاجتماعي
  url_launcher: ^6.2.1 # للاتصال بالنجار

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
